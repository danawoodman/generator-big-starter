var should = require('should');

describe('app', function () {
  before(function (done) {
    done();
  });

  it('test something', function () {
    var foo = true;
    foo.should.be.true;
  });
});

