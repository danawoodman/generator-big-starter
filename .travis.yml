language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-big-starter' ]; then cd .. && eval "mv $currentfolder generator-big-starter" && cd generator-big-starter; fi
